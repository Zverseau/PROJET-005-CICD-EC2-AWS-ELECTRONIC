services:

  mysql:
    image: mysql:8
    container_name: electronic_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: z_verseau@1234
      MYSQL_DATABASE: electronic_db
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - spring-network

    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  springboot-app:
    image: zbestleboss/electronic_backend
    container_name: electronicbackend
    restart: always
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - spring-network

  angular-app:
    image: zbestleboss/electronic-admin-frontend
    container_name: electronicfrontend
    restart: always
    ports:
      - "4200:80"
    depends_on:
      - springboot-app
    networks:
      - spring-network


networks:
  spring-network:
    driver: bridge

volumes:
  mysql_data: